You can remove the frontend directories before plugging-in the module into your architecture. 
As this module is built as part of an hackathon, we have to make those directories to demonstrate the module so you can safely remove them
and update these files in the node_backend directory to match your systems.
